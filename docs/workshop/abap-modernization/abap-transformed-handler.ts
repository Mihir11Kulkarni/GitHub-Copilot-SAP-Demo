// Converted via prompt from ABAP snippet (pseudo CAP code)
/*
const rows = await cds.run(SELECT.from(Product)
  .columns('ID','Name','Stock')
  .where({ Plant: '0001' })
  .orderBy({ Stock: 'desc' })
  .limit(50))
*/
export {}
